# codex-connector-test Status

Status: ARCHIVE / TEST-FIXTURE / DELETE CANDIDATE

codex-connector-test is a minimal public repository for testing Codex GitHub connector behavior.

It is not RTS core.

It is not RTS-AGE.

It is not a product repository.

It is not a component shelf.

It is not a production repository.

## Current Position

This repository has served as a sandbox-only connector test fixture.

It should not receive product work, RTS implementation work, component definitions, customer material, secrets, or runtime behavior.

Allowed by default:

- preserve test history
- document archive status
- document deletion readiness
- keep minimal connector-test notes
- confirm that no production responsibilities remain

Prohibited by default:

- adding production RTS behavior
- adding product logic
- adding customer information
- adding secrets, tokens, credentials, or private links
- adding external system mutation logic
- adding publishing flows
- importing implementations from RTS, Skills, MCP Packs, Hermes, Talent Registry, or Signal Feeds

## Archive Definition

This repository is ready for archive or deletion when:

1. No active project depends on it.
2. No useful connector-test evidence needs to be preserved here.
3. No secrets, credentials, or private material are present.
4. Any useful lessons have been copied into `rts-dev-protocol` or another appropriate repository.
5. A final human decision chooses archive or delete.

## Current Decision

Do not expand this repository.

Treat it as an archive/delete candidate.

Do not delete it automatically from this PR.
