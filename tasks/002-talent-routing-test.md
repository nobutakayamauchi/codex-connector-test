# 002 talent routing test

## Goal
Verify Codex can read `registry/talent-registry.yaml` and select reference candidates for:
- code review
- skill design
- MCP discovery

## Steps
1. Read `registry/talent-registry.yaml`.
2. Propose candidate mapping for each category.
3. Record why each candidate is only reference/candidate (not authority).

## Expected output
- A concise routing decision note in `results/talent-routing-result.md`.
